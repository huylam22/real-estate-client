import React from "react";

const PropertyMeta = ({ children }) => {
  return (
    <span className="font-semibold text-xl w-[100px] font-serif">
      {children}
    </span>
  );
};

export default PropertyMeta;
